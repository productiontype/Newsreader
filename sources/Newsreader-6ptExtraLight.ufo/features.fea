include(features/languagesystems-big.fea);
include(features/classes.fea);

include(features/features_locl.fea);
include(features/features_ordn.fea);
include(features/features_sups.fea);
include(features/features_pnum.fea);
include(features/features_tnum.fea);
include(features/features_case.fea);
include(features/features_liga.fea);